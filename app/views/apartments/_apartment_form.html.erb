<div class='bg-gray-lighter'>
   <h3 class='text-center'> <%= title %> </h3>
   <%= simple_form_for apartment, html: { class: 'form-horizontal' } do |f| %>

      <%= f.input :number %>
      <%= f.input :password, required: false %>
      <%= f.input :password_confirmation, required: false %>

      <h4>Status Information</h4>

      <%= f.fields_for :status do |s| %>
         <label class='checkbox'>
            <%= s.input :occupied, label: false %> Occupied?
         </label> <br />
         <%= status_start_date_label(apartment, s) %>
         <%= s.input :status_start_date, label: false, as: :string %>
         <%= s.input :comment %>
      <% end %>

      <%= f.submit "Submit", class: 'btn btn-primary' %>

   <% end %>
</div>


<!-- Being used to test Capybara javascript drivers -->

<div id="foobar">
<button id="foo-btn"> Click </button>
<div id="foo-text"> foo </div>
</div>


<script>
   $(document).ready(function() {
      $("#foobar").on('click', "#foo-btn", function() {
         $("#foobar").append("<p id='foo-p'>paragraph</p>")
      })
   })
</script>
