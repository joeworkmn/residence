<% if @ticket.destroyed? %>
   // Must use .get(0) because jquery returns an array of rows, 
   // but fnDeleteRow expects a single row.
   row = $("tr#" + '<%= dom_id(@ticket) %>').get(0)
   $(".data-table-gem").dataTable().fnDeleteRow(row)
<% end %>
