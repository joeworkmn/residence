<%= form_for(@ticket) do |f| %>
   <%= f.label :description %>
   <%= f.text_field :description %>

   <%= f.select :violation_ids, Violation.all.map { |v| [v.name, v.id] }, {}, multiple: true %>

   <%= f.submit "Submit" %>

<% end %>
