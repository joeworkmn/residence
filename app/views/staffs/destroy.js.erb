$("#main-flash-messages").html("<%= j render 'layouts/flash_messages' %>")
<% if @staff.destroyed? %>
   <% rowId = dom_id(@staff) %>
   row = $("tr#<%= rowId %>").get(0)

   $(".data-table-gem").dataTable().fnDeleteRow(row)
<% end %>
