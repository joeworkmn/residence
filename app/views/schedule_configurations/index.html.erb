<%= form_for @sch_config do |f| %>
   <%= f.label :default_interval_length, "Default rotation interval length" %>
   <%= f.text_field :default_interval_length %>

   <br />
   <%= f.submit "Submit", class: 'btn btn-primary' %>
<% end %>
