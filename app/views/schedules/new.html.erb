<div class='row'>
   <div class='span'>
      <%= form_for :schedule, url: schedules_path  do |f| %>
         <% c = 1 %>
         <% @intervals.each do |i| %>

            <div class='row-with-margin'>
               <div class="bg-gray-lighter">
               <%= f.fields_for "interval" + c.to_s do |b| %>
                  <%= b.text_field :foo %> <br />
                  <%= b.hidden_field :dates, value: i.dates %>
               <% end %>
               <%= i.range_text %>
               </div>
            </div>

            <% c += 1 %>
         <% end %>

         <%= f.submit "Submit" %>
      <% end %>
   </div>
</div>
